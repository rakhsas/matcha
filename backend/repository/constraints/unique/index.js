import { addUniqueConstraint as add, removeDuplicates, constraintExists as isConstraintExisting } from './add.unique.js';
import remove from './remove.unique.js';

export {
  add,
  remove,
  removeDuplicates,
  isConstraintExisting
};
